# Aurelian & Co

Generated by Relay from an immutable design package. This repository is build output, not a
hand-written application: the source here is what was reviewed and approved, and regenerating it
from the same package produces the same review.

## Provenance

| | |
|---|---|
| Run | `run-20260830014102-a81743` |
| Client | Aurelian & Co |
| Tier | standard |
| Design package | `aurelian-chrome-pkg` v1 (`pkg-recovered-run-20260830014102-a81743`) |
| Intake score | 0% against Contract 1.0 |
| Gate 2 — structure | not recorded |
| Gate 3 — build | not recorded |
| QA | failed — 17 blocking, 19 advisory (attempt 3) |
| Bundled | 2026-08-30T17:19:19.043Z |

`relay-provenance.json` carries the same record in machine-readable form. Do not edit it; it is what
ties a deployed URL back to the package that authorised it.

## Screens

- `/home` — Home (built)


## Running it

```bash
npm install
npm run dev
```

## Instrumentation

Every interactive element carries `data-analytics-event`, `data-testid`, and `data-src`. They come
from one instrumentation pass over this source, so an event in the data layer resolves back to the
file and symbol that emitted it.
